# Socket.IO signaling server URL (used for WebRTC signaling)
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001

# hCaptcha site key for bot protection
# Get your site key from https://www.hcaptcha.com/
NEXT_PUBLIC_HCAPTCHA_SITE_KEY=your-hcaptcha-site-key-here

# Ably Real-time Chat
# NOTE: Ably API key is hardcoded in src/components/ChatComponent.tsx
# NEXT_PUBLIC_ABLY_API_KEY=your-ably-api-key-here (not needed - hardcoded)

# Supabase (for chat message logging and authentication)
# NOTE: Supabase credentials are hardcoded in src/lib/supabase.ts
# NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url (not needed - hardcoded)
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-supabase-anon-key (not needed - hardcoded)
# Direct database connection URL (optional, for server-side operations)
SUPABASE_DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres
